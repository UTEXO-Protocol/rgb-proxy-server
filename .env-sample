APP_DIR='.rgb-proxy-server'

# Offline receiver: server-side consignment validation
# Set INDEXER_URL to enable automatic validation (esplora endpoint)
#INDEXER_URL='http://localhost:3002'
# Possible values: Mainnet, Testnet, Signet, Regtest
#BITCOIN_NETWORK='Testnet'
