APP_DIR='.rgb-proxy-server'

# Consignment validation (optional — omit to disable, falls back to relay-only)
#ELECTRUM_URL=ssl://electrum.blockstream.info:60002
# Accepted values (case-insensitive): mainnet, bitcoin, testnet, testnet3, testnet4, signet, regtest
#BITCOIN_NETWORK=mainnet